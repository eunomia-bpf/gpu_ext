# Stage 4 Eviction Policy Safety Audit

Evidence class: `SOURCE_STATIC_AUDIT`.

| Policy | Hooks | Kfuncs | Combined | Decision | Reasons |
|---|---|---|---|---|---|
| `eviction_cycle_moe` | gpu_block_access, gpu_block_activate, gpu_evict_prepare | bpf_gpu_block_move_tail | no | `REJECTED_INITIAL_STAGE4` | not in the explicitly scoped Stage 4 initial candidate set; behavior depends on gpu_block_access, which is not a reliable current hook |
| `eviction_fifo` | gpu_block_access, gpu_block_activate, gpu_evict_prepare | none | no | `REJECTED_INITIAL_STAGE4` | contains bpf_printk in a memory-management hot path; behavior depends on gpu_block_access, which is not a reliable current hook; FIFO comments claim list ordering but the implementation does not reorder chunks |
| `eviction_fifo_chance` | gpu_block_access, gpu_block_activate, gpu_evict_prepare | bpf_gpu_block_move_tail | no | `REJECTED_INITIAL_STAGE4` | not in the explicitly scoped Stage 4 initial candidate set; behavior depends on gpu_block_access, which is not a reliable current hook |
| `eviction_freq_pid_decay` | gpu_block_access, gpu_block_activate, gpu_evict_prepare | bpf_gpu_block_move_tail | no | `REJECTED_INITIAL_STAGE4` | not in the explicitly scoped Stage 4 initial candidate set; behavior depends on gpu_block_access, which is not a reliable current hook |
| `eviction_lfu` | gpu_block_access, gpu_block_activate, gpu_evict_prepare | bpf_gpu_block_insert_after, bpf_gpu_block_move_head, bpf_gpu_block_move_tail | no | `REJECTED_INITIAL_STAGE4` | not in the explicitly scoped Stage 4 initial candidate set; gpu_block_activate calls bpf_gpu_block_move_head; contains bpf_printk in a memory-management hot path; behavior depends on gpu_block_access, which is not a reliable current hook |
| `eviction_lfu_xcoord` | gpu_block_access, gpu_block_activate, gpu_evict_prepare | bpf_gpu_block_move_tail | no | `REJECTED_INITIAL_STAGE4` | not in the explicitly scoped Stage 4 initial candidate set; behavior depends on gpu_block_access, which is not a reliable current hook |
| `eviction_mru` | gpu_block_access, gpu_block_activate, gpu_evict_prepare | bpf_gpu_block_move_head, bpf_gpu_block_move_tail | no | `REJECTED_INITIAL_STAGE4` | not in the explicitly scoped Stage 4 initial candidate set; contains bpf_printk in a memory-management hot path; behavior depends on gpu_block_access, which is not a reliable current hook |
| `eviction_pid_quota` | gpu_block_access, gpu_block_activate, gpu_evict_prepare | bpf_gpu_block_move_tail | no | `REJECTED_INITIAL_STAGE4` | not in the explicitly scoped Stage 4 initial candidate set; behavior depends on gpu_block_access, which is not a reliable current hook |
| `gpu_sched_set_timeslices` | on_bind, on_task_destroy, on_task_init | none | no | `REJECTED_INITIAL_STAGE4` | not in the explicitly scoped Stage 4 initial candidate set; contains bpf_printk in a memory-management hot path |
| `prefetch_adaptive_sequential` | gpu_page_prefetch, gpu_page_prefetch_iter, gpu_test_trigger | bpf_gpu_set_prefetch_region | no | `REJECTED_INITIAL_STAGE4` | not in the explicitly scoped Stage 4 initial candidate set; contains bpf_printk in a memory-management hot path |
| `prefetch_adaptive_tree_iter` | gpu_page_prefetch, gpu_page_prefetch_iter, gpu_test_trigger | bpf_gpu_set_prefetch_region | no | `REJECTED_INITIAL_STAGE4` | not in the explicitly scoped Stage 4 initial candidate set; contains bpf_printk in a memory-management hot path |
| `prefetch_always_max` | gpu_page_prefetch, gpu_page_prefetch_iter, gpu_test_trigger | bpf_gpu_set_prefetch_region | no | `REJECTED_INITIAL_STAGE4` | not in the explicitly scoped Stage 4 initial candidate set; contains bpf_printk in a memory-management hot path |
| `prefetch_always_max_cycle_moe` | gpu_block_access, gpu_block_activate, gpu_evict_prepare, gpu_page_prefetch, gpu_page_prefetch_iter, gpu_test_trigger | bpf_gpu_block_move_tail, bpf_gpu_set_prefetch_region | yes | `APPROVED_FOR_SMOKE` | none |
| `prefetch_always_max_qos` | gpu_block_access, gpu_block_activate, gpu_evict_prepare, gpu_page_prefetch, gpu_page_prefetch_iter, gpu_test_trigger | bpf_gpu_block_move_head, bpf_gpu_block_move_tail, bpf_gpu_set_prefetch_region | yes | `REJECTED_INITIAL_STAGE4` | not in the explicitly scoped Stage 4 initial candidate set; gpu_block_activate calls bpf_gpu_block_move_head |
| `prefetch_always_max_xcoord` | gpu_block_access, gpu_block_activate, gpu_evict_prepare, gpu_page_prefetch, gpu_page_prefetch_iter, gpu_test_trigger | bpf_gpu_block_move_tail, bpf_gpu_set_prefetch_region | yes | `REJECTED_INITIAL_STAGE4` | not in the explicitly scoped Stage 4 initial candidate set |
| `prefetch_cooperative` | gpu_block_access, gpu_block_activate, gpu_evict_prepare, gpu_page_prefetch, gpu_page_prefetch_iter, gpu_test_trigger | bpf_gpu_block_move_tail, bpf_gpu_migrate_range, bpf_gpu_set_prefetch_region | yes | `REJECTED_INITIAL_STAGE4` | uses bpf_wq; bounded completion and context safety are not proven; performs cross-VA-block migration via bpf_gpu_migrate_range |
| `prefetch_cross_block_v2` | gpu_block_access, gpu_block_activate, gpu_evict_prepare, gpu_page_prefetch, gpu_page_prefetch_iter, gpu_test_trigger | bpf_gpu_block_move_tail, bpf_gpu_migrate_range, bpf_gpu_set_prefetch_region | yes | `REJECTED_INITIAL_STAGE4` | not in the explicitly scoped Stage 4 initial candidate set; uses bpf_wq; bounded completion and context safety are not proven; performs cross-VA-block migration via bpf_gpu_migrate_range |
| `prefetch_eviction_pid` | gpu_block_access, gpu_block_activate, gpu_evict_prepare, gpu_page_prefetch, gpu_page_prefetch_iter, gpu_test_trigger | bpf_gpu_block_move_tail, bpf_gpu_set_prefetch_region | yes | `REJECTED_INITIAL_STAGE4` | not in the explicitly scoped Stage 4 initial candidate set; contains bpf_printk in a memory-management hot path |
| `prefetch_faiss_phase` | gpu_block_access, gpu_block_activate, gpu_evict_prepare, gpu_page_prefetch, gpu_page_prefetch_iter, gpu_test_trigger | bpf_gpu_migrate_range, bpf_gpu_set_prefetch_region | yes | `REJECTED_INITIAL_STAGE4` | not in the explicitly scoped Stage 4 initial candidate set; uses bpf_wq; bounded completion and context safety are not proven; performs cross-VA-block migration via bpf_gpu_migrate_range |
| `prefetch_faiss_uprobe` | gpu_block_access, gpu_block_activate, gpu_evict_prepare, gpu_page_prefetch, gpu_page_prefetch_iter, gpu_test_trigger | bpf_gpu_migrate_range, bpf_gpu_set_prefetch_region | yes | `REJECTED_INITIAL_STAGE4` | not in the explicitly scoped Stage 4 initial candidate set; uses bpf_wq; bounded completion and context safety are not proven; performs cross-VA-block migration via bpf_gpu_migrate_range |
| `prefetch_gnn_proactive` | gpu_block_access, gpu_block_activate, gpu_evict_prepare, gpu_page_prefetch, gpu_page_prefetch_iter, gpu_test_trigger | bpf_gpu_block_move_tail, bpf_gpu_migrate_range, bpf_gpu_set_prefetch_region | yes | `REJECTED_INITIAL_STAGE4` | not in the explicitly scoped Stage 4 initial candidate set; uses bpf_wq; bounded completion and context safety are not proven; performs cross-VA-block migration via bpf_gpu_migrate_range |
| `prefetch_llama_phase` | gpu_block_access, gpu_block_activate, gpu_evict_prepare, gpu_page_prefetch, gpu_page_prefetch_iter, gpu_test_trigger | bpf_gpu_block_move_tail, bpf_gpu_migrate_range, bpf_gpu_set_prefetch_region | yes | `REJECTED_INITIAL_STAGE4` | not in the explicitly scoped Stage 4 initial candidate set; uses bpf_wq; bounded completion and context safety are not proven; performs cross-VA-block migration via bpf_gpu_migrate_range |
| `prefetch_max_mru_expert` | gpu_block_access, gpu_block_activate, gpu_evict_prepare, gpu_page_prefetch, gpu_page_prefetch_iter, gpu_test_trigger | bpf_gpu_block_move_head, bpf_gpu_block_move_tail, bpf_gpu_set_prefetch_region | yes | `REJECTED_INITIAL_STAGE4` | not in the explicitly scoped Stage 4 initial candidate set; gpu_block_activate calls bpf_gpu_block_move_head |
| `prefetch_max_passive_mru` | gpu_block_access, gpu_block_activate, gpu_evict_prepare, gpu_page_prefetch, gpu_page_prefetch_iter, gpu_test_trigger | bpf_gpu_block_move_tail, bpf_gpu_set_prefetch_region | yes | `REJECTED_INITIAL_STAGE4` | not in the explicitly scoped Stage 4 initial candidate set |
| `prefetch_moe_expert` | gpu_block_access, gpu_block_activate, gpu_evict_prepare, gpu_page_prefetch, gpu_page_prefetch_iter, gpu_test_trigger | bpf_gpu_block_move_tail, bpf_gpu_migrate_range, bpf_gpu_set_prefetch_region | yes | `REJECTED_INITIAL_STAGE4` | not in the explicitly scoped Stage 4 initial candidate set; uses bpf_wq; bounded completion and context safety are not proven; performs cross-VA-block migration via bpf_gpu_migrate_range |
| `prefetch_none` | gpu_page_prefetch, gpu_page_prefetch_iter, gpu_test_trigger | bpf_gpu_set_prefetch_region | no | `REJECTED_INITIAL_STAGE4` | not in the explicitly scoped Stage 4 initial candidate set; contains bpf_printk in a memory-management hot path |
| `prefetch_pid_tree` | gpu_page_prefetch, gpu_page_prefetch_iter, gpu_test_trigger | bpf_gpu_set_prefetch_region | no | `REJECTED_INITIAL_STAGE4` | not in the explicitly scoped Stage 4 initial candidate set; contains bpf_printk in a memory-management hot path |
| `prefetch_proactive_layer` | gpu_block_access, gpu_block_activate, gpu_evict_prepare, gpu_page_prefetch, gpu_page_prefetch_iter, gpu_test_trigger | bpf_gpu_block_move_tail, bpf_gpu_migrate_range, bpf_gpu_set_prefetch_region | yes | `REJECTED_INITIAL_STAGE4` | not in the explicitly scoped Stage 4 initial candidate set; uses bpf_wq; bounded completion and context safety are not proven; performs cross-VA-block migration via bpf_gpu_migrate_range |
| `prefetch_reuse_dist` | gpu_block_access, gpu_block_activate, gpu_evict_prepare, gpu_page_prefetch, gpu_page_prefetch_iter, gpu_test_trigger | bpf_gpu_block_move_tail, bpf_gpu_migrate_range, bpf_gpu_set_prefetch_region | yes | `REJECTED_INITIAL_STAGE4` | not in the explicitly scoped Stage 4 initial candidate set; uses bpf_wq; bounded completion and context safety are not proven; performs cross-VA-block migration via bpf_gpu_migrate_range |
| `prefetch_serving_adaptive` | gpu_page_prefetch, gpu_page_prefetch_iter, gpu_test_trigger | bpf_gpu_set_prefetch_region | no | `REJECTED_INITIAL_STAGE4` | not in the explicitly scoped Stage 4 initial candidate set |
| `prefetch_stride` | gpu_page_prefetch, gpu_page_prefetch_iter, gpu_test_trigger | bpf_gpu_set_prefetch_region | no | `REJECTED_INITIAL_STAGE4` | not in the explicitly scoped Stage 4 initial candidate set; contains bpf_printk in a memory-management hot path |
| `prefetch_stride_multiblock` | gpu_block_access, gpu_block_activate, gpu_evict_prepare, gpu_page_prefetch, gpu_page_prefetch_iter, gpu_test_trigger | bpf_gpu_block_move_tail, bpf_gpu_migrate_range, bpf_gpu_set_prefetch_region | yes | `REJECTED_INITIAL_STAGE4` | not in the explicitly scoped Stage 4 initial candidate set; uses bpf_wq; bounded completion and context safety are not proven; performs cross-VA-block migration via bpf_gpu_migrate_range |
| `prefetch_template_belady` | gpu_block_access, gpu_block_activate, gpu_evict_prepare, gpu_page_prefetch, gpu_page_prefetch_iter, gpu_test_trigger | bpf_gpu_block_move_head, bpf_gpu_block_move_tail, bpf_gpu_set_prefetch_region | yes | `REJECTED_INITIAL_STAGE4` | not in the explicitly scoped Stage 4 initial candidate set; gpu_block_activate calls bpf_gpu_block_move_head |
| `prefetch_throttled_xb` | gpu_block_access, gpu_block_activate, gpu_evict_prepare, gpu_page_prefetch, gpu_page_prefetch_iter, gpu_test_trigger | bpf_gpu_block_move_tail, bpf_gpu_migrate_range, bpf_gpu_set_prefetch_region | yes | `REJECTED_INITIAL_STAGE4` | not in the explicitly scoped Stage 4 initial candidate set; uses bpf_wq; bounded completion and context safety are not proven; performs cross-VA-block migration via bpf_gpu_migrate_range |
| `prefetch_vllm_phase` | gpu_block_access, gpu_block_activate, gpu_evict_prepare, gpu_page_prefetch, gpu_page_prefetch_iter, gpu_test_trigger | bpf_gpu_block_move_tail, bpf_gpu_migrate_range, bpf_gpu_set_prefetch_region | yes | `REJECTED_INITIAL_STAGE4` | not in the explicitly scoped Stage 4 initial candidate set; uses bpf_wq; bounded completion and context safety are not proven; performs cross-VA-block migration via bpf_gpu_migrate_range |
| `prefetch_vllm_phase_transparent` | gpu_block_access, gpu_block_activate, gpu_evict_prepare, gpu_page_prefetch, gpu_page_prefetch_iter, gpu_test_trigger | bpf_gpu_block_move_tail, bpf_gpu_migrate_range, bpf_gpu_set_prefetch_region | yes | `REJECTED_INITIAL_STAGE4` | not in the explicitly scoped Stage 4 initial candidate set; uses bpf_wq; bounded completion and context safety are not proven; performs cross-VA-block migration via bpf_gpu_migrate_range |
| `struct_ops` | gpu_test_trigger | bpf_gpu_strstr | no | `REJECTED_INITIAL_STAGE4` | not in the explicitly scoped Stage 4 initial candidate set; contains bpf_printk in a memory-management hot path |
| `test_chunk_access` | gpu_block_access, gpu_block_activate, gpu_evict_prepare | none | no | `REJECTED_INITIAL_STAGE4` | not in the explicitly scoped Stage 4 initial candidate set; contains bpf_printk in a memory-management hot path |
| `test_uprobe_prefetch` | gpu_page_prefetch, gpu_page_prefetch_iter, gpu_test_trigger | bpf_gpu_migrate_range, bpf_gpu_set_prefetch_region | no | `REJECTED_INITIAL_STAGE4` | not in the explicitly scoped Stage 4 initial candidate set; uses bpf_wq; bounded completion and context safety are not proven; performs cross-VA-block migration via bpf_gpu_migrate_range; contains bpf_printk in a memory-management hot path |

## Enforcement

Only policies marked `APPROVED_FOR_SMOKE` may enter the 64 MiB and 0.95x smoke gates. Static approval does not grant permission to skip the runtime safety checks.
