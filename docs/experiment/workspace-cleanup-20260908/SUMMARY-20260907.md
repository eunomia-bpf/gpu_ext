# Disk cleanup summary

User authorized removal of unused files. Completed 2026-09-07T22:08:10.315112-07:00.

Approximate available space from df -h:
- System filesystem: 56 GiB before, 113 GiB after (94% -> 88%).
- Workspace filesystem: 342 GiB before, 593 GiB after (82% -> 68%).

Completed:
- Removed 40.51 GiB of archived regenerable LMCache KV payloads under /var/tmp; retained metadata and all original experiment records.
- Removed 197.66 GiB of finished LMCache run cache payloads, keeping result files and all measurement logs/metadata. Current total-cost campaign directory was excluded.
- Removed 8.58 GiB of old generated .pt KV cache payloads.
- Removed 14.06 GiB of duplicate plain JSONL files only after byte-by-byte comparison with retained compressed copies. Existing analyzer supports gzip input.
- Emptied both user trash locations and unused npm/pip/APT download caches.
- Removed inactive npx/Bun/Cargo cache entries older than seven days, unused Go build cache, and Docker build cache older than 24 hours (Docker reported 4.206 GB reclaimed).
- Removed seven inactive old Codex installations, preserving current and previous releases. Current Codex command works.
- Removed eleven disabled Snap revisions after checking for running executables/mappings.
- Compressed four old crash reports without dropping diagnostic content.

Preserved: source repositories, paper figures/data, models, experiment results, chat exports, Codex/OpenCode histories, active environments, current driver/modules and running tasks. Hourly experiment-progress timer remains active.

Detailed KV and duplicate-log inventories are adjacent to this summary. The retired archive also contains its own CLEANUP.md.
