# OpenCode read-only review: expressibility evidence refresh

- Model: `opencode/ling-3.0-flash-fin-free`
- Session: `ses_f94e6a4d5ffewWvaQjvTJ02Zdr`
- Mode: read-only; snapshot and sharing disabled; write, edit, shell,
  network, and delegation permissions denied
- Verdict: **PASS**; no factual or claim-boundary blocker

The reviewer checked the refreshed Markdown ledger and 48-record JSON inventory
against the completed MoE-Infinity, XSched, GPREEMPT, Expert Buffering,
FineMoE, Hummingbird, POD-Attention, UVM same-policy, device-trampoline,
strict-callback, and raw-map reports.  It independently matched the reported
cell counts, medians, ratios and intervals, and accepted the distinction
between whole-system `PARTIAL`/`ANALOGUE` classifications and the strongest
local `performance` evidence level.

The review also confirmed that native means a matching non-BPF policy port
unless the original runtime is explicitly retained, that no result is promoted
to formal equivalence or full-system reproduction, and that strict callback
engagement is separate from verification-disabled trampoline timing.  LMCache
remains paused with no formal performance or BPF arm, and RTX 5090 Table 1
remains incomplete.

Local checks run before and after review:

- `validate_related_policy_expressibility.py`: 48 records across all seven
  required categories accepted.
- JSON syntax and whitespace checks: passed.
- Repository-relative Markdown links: all resolve; zero missing.
- Obsolete build/source-only phrases for the five promoted rows: absent.
