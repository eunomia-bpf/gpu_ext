# Interrupted paper-policy campaign: 2026-09-03

The machine's current boot began at **2026-09-03 01:37:41 UTC**. A read-only
recovery audit at 01:46 UTC found no surviving paper-policy server or comparison
runner. The campaign directory is
`raw/paper-v3-575/timing-849ea75d-01`; its first attempt is
`block-01-attempt-01`. No second attempt or complete block result existed at
audit time. The root cause of the restart and lost raw data is not established
by this audit.

The result is **zero complete, reusable paired blocks out of five**, not a
completed three-way comparison. Nothing in the interrupted attempt was replaced
or reconstructed from the producer's result summaries.

## Persisted observations

| Cell | Producer summary | Independent raw-data audit |
| --- | --- | --- |
| `paper-bpf` | 512 verified output tokens in 45.724326613 s; 11.1975405200 token/s; server exit 0; no cleanup errors | Pass: all eight raw SSE responses, exact frozen texts, timing, telemetry, activation, engagement and teardown observations |
| `paper-native` | 512 output tokens in 45.453740112 s; 11.2641995739 token/s; server exit 0; no cleanup errors | **Fail**: three raw responses and retained telemetry do not support the summary |
| `native-off` | No result, warmup or requests | Not completed; only admission, launch and an empty server log persisted |

Both completed producer summaries use the same frozen prompt order
`6, 5, 1, 7, 4, 2, 3, 8`, fresh frontend processes, one excluded warmup,
512 input and 64 output tokens per measured request, and a primary wall window
including final prefetch drain. They are not sufficient for a paired performance
claim: the baseline is missing and the native raw cell fails independent audit.
In particular, do not use these two producer numbers to claim BPF parity.

The native cell's retained data problems are concrete:

- `request-06-prompt-2.sse`: 11,083 bytes, all NUL bytes; no recoverable SSE frame.
- `request-07-prompt-3.sse`: 0 bytes; its JSON declares 11,094 bytes and 65 frames.
- `request-08-prompt-8.sse`: 0 bytes; its JSON declares 11,084 bytes and 65 frames.
- `gpu-telemetry.csv`: 24,576 bytes and 186 parseable samples, whereas the result
  declares 229 samples. Recomputed mean power is 220.3813440860215 W versus the
  producer's 219.64017467248908 W. Therefore even ignoring the missing responses
  would not make the retained cell valid.

The first five native responses, native activation and engagement snapshots, and
native server log pass their individual checks. That does not validate the
entire cell. The BPF cell passes the complete strict cell audit; it remains an
unpaired diagnostic observation only.

## Audit and retry policy

Audited with `paper_result_audit.py` at repository commit
`d3f3998b4f121791c62e1c4cbc1c2ba866f1ba11`:

1. `_runtime(manifest["runtime_inventory"])` passes against current revisions,
   file metadata, model inventory, prompt corpus and golden responses. This
   comparison does not prove which driver modules are currently loaded.
2. `_cell(...)` passes for `paper-bpf` and rejects `paper-native` at the malformed
   sixth response. Individual request and telemetry checks expose the additional
   losses listed above.
3. `audit_block(...)` rejects the interrupted attempt because its top-level
   `result.json` is absent. `run_paper_comparison.analyze([])` returns
   `valid_blocks: 0`, `complete: false`, and no performance comparisons.

The coordinator selected a new campaign directory,
`raw/paper-v3-575/timing-849ea75d-02-postboot`, because the runner will gain
explicit artifact persistence after all writers close and outside the timed
window. This changes the recorded runtime inventory, so the old manifest must
not be edited to permit reuse. The coordinator must first restore the declared
`/opt/gpubpf/modules/575.57.08/gpreempt-849ea75d-6.15.11` driver and verify the
normal idle-GPU, 400 W and uncontended timing requirements.

```bash
cd /home/yunwei37/workspace/gpu/gpu_ext/workloads/moe-infinity
.venv/bin/python -B run_paper_comparison.py \
  --output raw/paper-v3-575/timing-849ea75d-02-postboot \
  --driver-stage /opt/gpubpf/modules/575.57.08/gpreempt-849ea75d-6.15.11
```

This is a recovery instruction, not a claim that the retry ran. The runner will
retain the entire original directory and run all three cells in a fresh first
block using the originally scheduled order. It must not carry the pre-restart
BPF cell or native summary into a new block. All five complete paired blocks
remain required. GPU reloads and experiment launches remain coordinated by the
root agent; this audit performed neither.
