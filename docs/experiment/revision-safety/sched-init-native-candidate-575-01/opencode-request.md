You are an independent read-only systems reviewer. Do not call tools, edit
files, run commands, use the network, or infer that any GPU/module experiment
has run. The relevant files are attached directly.

Audit the proposed RTX 5090/575 scheduler-init native commit-path functional
experiment. Return `READY` or `REQUIRED FIXES`, with blockers first and exact
file/function references. Check:

1. Does the eight-cell, two-block matrix distinguish truly unattached native,
   attached no-request, legal/repeat, invalid/conflict and independent-field
   behavior without calling a CPU or load-only result native evidence?
2. Is the proposed address-free hook sufficient to prove the actual production
   validator result, host-setter return, final CPU state and constructor return
   without becoming a new policy/control path or changing existing errors?
3. Can the existing post-wait GSP hook be joined unambiguously by target
   PID/TID, hClient/hResource and the VALIDATED-to-CONSTRUCTOR_RETURN interval?
   Identify any missing identity, phase, early-return, lifetime or ordering
   case that could create false positive evidence.
4. Are the fixture-loader engagement gates and vector numerical oracle enough
   to separate policy request, transition decision, native setter and completed
   GSP control? Check every row's expected setters/final values.
5. Is the proposed full-core reload/restore boundary fail-closed and materially
   stronger than the UVM-only lifecycle? Flag any dependency, service, holder,
   device-node, signal, recording or restoration gap.
6. Is the final claim no broader than the evidence, especially around verifier
   rejection, performance, physical scheduling, universal semantics and crash
   recovery?

Do not require more workloads or repetitions unless the current design cannot
answer this functional hypothesis. Do not report file hashes, checksums,
fingerprints or digests. Keep the final review under 900 words.
