- Never generate, refresh, compare, or record file/content hashes, checksums,
  or digests for this workload, and never use them as experiment gates or
  evidence. Use normal Git revisions, exact small-file contents, resolved
  filenames, sizes, device/inode identities, and timestamps instead. Git
  commit IDs and upstream source revisions remain normal bookkeeping.
