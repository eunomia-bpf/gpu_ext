

**READY** (blocker 1 retracted)

**Retraction.** Direct evidence disproves my blocker: this host exposes both live module BTF files as `mode=444` root:root and an unprivileged `bpftool btf dump` exited 0 (live-btf-access.md:7-9); the coordinator preflight ran `capture_core()` and `live_uvm_interface(diagnostic=False)` as UID 1000 with `core.btf.mode=292` (=0444) recorded (root-readonly-preflight.json:101) and `complete: true`. The no-`sudo` `btf_raw` (run_lifecycle.py:172) is a host-portability fragility, not a readiness blocker for this host, which the plan pins. The same preflight also confirms every admission shape the code requires: leases root:root 0644 (mode 420, lines 120, 129), 53-name parameter inventory match (line 136), candidate 61,919,280 / restore 61,914,016 bytes, stage/output absent, only remote sshd + local greeter sessions, `gdm.service` static / persistenced enabled, zero refcount, empty struct_ops, 400 W, idle GPU.

**Six-part completion:**

1. **Leases:** acquired once via `LifecycleLeases` with `O_RDONLY|O_CLOEXEC|O_NOFOLLOW`, no create/write (run_lifecycle.py:61-63; test_lifecycle.py:98-99); partial failure releases earlier fds (67-79; test 67-81); identity verified before, while held, and at publication (896, 920-922, 767-768); closed before any record write (775-776 precedes 812; test 443-445); `run_safety.main` is never invoked, so no second acquisition.
2. **Fail-closed gates:** admission, pre-removal (two idle snapshots + kernel-history delta + silent-exit-1 `fuser`, 549-565), post-insert ABI/parameter equality (579-585), session recheck persisted immediately before GDM stop (409-415) — all raise `LifecycleError`.
3. **Recovery:** classifier removes only the candidate (588-597); old module revalidated as artifact then live-validated (ABI, parameters, core continuity, idle, holders, boot) before any service start (661-664, 684-718); otherwise services withheld (729-731); record failures never block the physical sequence (test 360-370).
4. **Signals:** recovery ignores queued signals by design; publication blocks both signals and takes queued∪pending (785-790), closing the lease-close-to-mask window; second snapshot is the commit point (838-852); every write-failure path (`final_record`, `summary_record`, `completion_commit`, `summary_reject`, `rejected_record`) demotes `eligible`; handlers restored only under the mask (888-892; tests 437-507). No `complete: true` lifecycle/summary can precede lease close or the commit point.
5. **Forbidden operations:** unreachable — plain `rmmod` (561), explicit-path `insmod` (303-304), no `modprobe`/`depmod`/`reboot`/force anywhere; `systemctl` restricted to start/stop of the two fixed units (335-347); `killpg` only escalates against the coordinator's own bounded-timeout children (124-137).
6. **Tests/claims:** CPU failure-injection tests substantively cover the claims; the plan explicitly disclaims live execution (prefetch-uvm-lifecycle-plan.md:3-4, 101-102).

**Remaining limitations (non-blocking):** shared `safety.atomic_write_json` internals not attached (durability assumed); crash window between the `complete: true` lifecycle write and summary promotion (ordering is safe direction, lifecycle-first); pre-staging failures force the "staged candidate never passed" final-validation error so `restored` is never reported true (conservative); the 30 s `STATE_TIMEOUT` may reject a slow GDM restart fail-closed; a recorder write failure inside the body exception handler (1013) escapes into the `finally` chain and drops the `campaign_body failed` transition entry.
