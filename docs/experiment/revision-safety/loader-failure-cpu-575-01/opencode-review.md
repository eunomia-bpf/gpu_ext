# OpenCode read-only review

Date: 2026-09-04

- Client: OpenCode 1.18.27
- Model: `opencode/ling-3.0-flash-fin-free`
- Session: `ses_f956ec3c4ffes10MCQZaP2E7dC`
- Configuration: snapshots and sharing disabled; write, edit, shell, web-fetch,
  task, and every unspecified permission denied.
- Verdict: **PASS**, with no blocking findings.

The reviewer confirmed that the harness enters the interposed
`BPF_PROG_LOAD` path, that the invalid/control pair and three repetitions
support the narrow no-program-slot-allocation result, that only explicit
`STRICT` is fail-closed, and that the CPU/GPU/kernel/driver exclusions are
adequate. It also confirmed that missing or unrelated output cannot satisfy
the result parser.

The reviewer suggested a final shared-memory existence check as optional
defense in depth. The retained runner already checks removal after every cell,
and the main-thread post-run check found no matching private object, so this is
not a result blocker.

After the final retained run was regenerated by the reviewed harness, a
follow-up in the same session inspected the final report, environment,
machine-checked summary, and representative final logs. It returned
**FINAL VERDICT: PASS** with no blocking findings. The response is retained in
`opencode-followup-events.jsonl`; its stderr is empty.

The complete response and session events are retained in
`opencode-events.jsonl`. The first CLI attempt is retained separately; option
placement made the prompt look like a file argument, so it exited before
creating a session or reviewing evidence.
