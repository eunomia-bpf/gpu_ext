# Requested label differs from the executed policy

This attempt completed with 66.9241699594 output token/s, but is NOT the
planned native cost-per-byte control. The raw configuration's
`native_ratio` label records intent, not the actual algorithm in this run.

The local GLM session assigned read-only analysis edited the shared header
and rebuilt `kv_reclaim_native.so` at 03:12:48 UTC, before this server
started at 03:13:59 UTC. Its unselected rule subtracts backed transfer
bytes from freeable bytes, excludes net-zero candidates, and ranks by
cost/net-freeable bytes. This run finished at 03:16:43 UTC, before root
restored the original algorithm at 03:18:22 UTC. Preserve it as an
unplanned native-net variant, never as an original-ratio performance row.

The patch is preserved in
`gds-control/unselected-net-freeable-20260908.patch`. The modified binaries
and header are recoverable under
`/var/tmp/lmcache-unselected-net-20260908.IyWgEq/`.
The total-cost BPF/native binaries were not rebuilt by that session.
Block 0 finished before the modification and is unaffected. A new
original-ratio cell is still needed for block 1; this completed unplanned
policy run will not be rerun or relabeled as a planned control.
