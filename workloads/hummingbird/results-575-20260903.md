# Hummingbird idle scheduling — complete, negative throughput result

The five-arm, two-arrival, five-block comparison completed all **50 cells** on
2026-09-03 at 10:53 UTC. The [raw analysis](raw/idle-study-575-01/analysis.json)
accepts all 50, with no missing, rejected or unexpected cells. Calibration,
20 small-pattern qualification cells and ten preflight cells remain excluded.

**The idle scheduler did not recover background throughput versus fixed
GPreempt.** C and BPF both lose about 19–20% on this frontend. This completes
the specified comparison, not a successful reproduction of Hummingbird's
headline benefit or proof of C/BPF equivalence.

## Formal results

Cell medians; response p99 is scheduled arrival to numerically verified
completion. BE goodput counts only verified completions within the 60-second
window. SLO attainment uses all 6,000 offered LC requests and the frozen
1.811879 ms threshold, not only successful requests.

| Arrival | Arm | LC p99, ms | BE goodput, req/s | SLO attainment, % |
| --- | --- | ---: | ---: | ---: |
| Periodic | Native | 1.979722 | 179.2333 | 76.8000 |
| Periodic | Fixed GPreempt | 1.870042 | 164.1500 | 98.7333 |
| Periodic | Equal-timeslice control | 6.544504 | 173.5333 | 5.8667 |
| Periodic | Idle C | 1.830785 | 133.2167 | 98.3000 |
| Periodic | Idle BPF | 1.864302 | 133.0500 | 97.5333 |
| BurstGPT | Native | 5.435775 | 179.1500 | 61.1167 |
| BurstGPT | Fixed GPreempt | 5.293070 | 164.6167 | 75.0500 |
| BurstGPT | Equal-timeslice control | 1994.244898* | 173.0333 | 0.4000 |
| BurstGPT | Idle C | 4.989155 | 133.1667 | 73.4833 |
| BurstGPT | Idle BPF | 4.939697 | 132.8167 | 72.8833 |

*The BurstGPT equal-timeslice control never started 312 of its 30,000 offered
requests; five more completed after their windows. Its p99 is conditional on
started requests and includes the verified late tail. Do not advertise a
roughly 400-fold protection gain using that incomplete-coverage denominator.
Every other arm has full in-window LC coverage. All 299,688 started LC and
468,560 started BE requests passed the retained full-output numerical checks.

Paired geometric ratios, with whole-block 95% bootstrap intervals
(10,000 draws, seed 20260903); these are not ratios of the medians above:

| Comparison | Periodic BE ratio [95% interval] | BurstGPT BE ratio [95% interval] |
| --- | --- | --- |
| Idle C / fixed | 0.81149 [0.80893, 0.81461] | 0.80160 [0.78132, 0.81413] |
| Idle BPF / fixed | 0.80685 [0.79792, 0.81333] | 0.79693 [0.77145, 0.81358] |
| Idle BPF / idle C | 0.99428 [0.98598, 0.99960] | 0.99417 [0.95256, 1.03643] |

BPF/C periodic LC-p99 ratio is 0.82526 [0.54485, 1.02067], heavily affected by
one C block's larger tail; four of five paired ratios exceed one. BurstGPT's
ratio is 1.02046 [0.96018, 1.09275]. Neither establishes BPF latency superiority.
BPF-minus-C mean SLO attainment is −1.7433 percentage points
[−3.4833, −0.7133] for periodic and −0.2333 [−1.1500, +0.8967] for BurstGPT.
Thus a small average throughput difference does not establish equal protection.
Neither idle implementation meets the predeclared joint recovery/protection
criterion against fixed GPreempt; no adverse block was discarded or retuned.

## Execution and fidelity

The unchanged full batch used the selected output-only profile and the
restored custom 575 / Linux 6.15.11 runtime, with the two shared leases, 400 W,
continuous telemetry, full-output checks and owned cleanup. The
[driver restoration](../../docs/experiment/driver-575-linux-6.15-runtime.md)
preceded this batch; the calibration below predates that restoration.
All BPF decisions used the actual **host uBPF JIT: 1,907,838,828 calls**; C
recorded 1,930,854,357 decisions and zero JIT calls. CTA coverage, completed
requests, no-op copies, real output-bubble launches and large-bubble
consolidation were checked in every idle cell. This is not device-side BPF.

The port implements the paper-described splitting/idle-admission component on
the GPreempt DNN frontend, not an author-released scheduler. Its common
executor permits only one in-flight LP launch and waits on a completion event,
unlike the paper's predicted pipeline. Event-query/pacing/submission costs are
candidate explanations for the shared C/BPF loss, not an isolated causal
measurement. Overlapping counters cannot be summed into a cost breakdown.
The experiment bounds this conservative port; it does not show that the
original Hummingbird system loses 20%.

All 355 full-campaign files are retained (149,198,131 bytes, including the
analysis); publication is tracked separately in the revision checklist.

## Completed calibration

The source/runtime was prepared at gpu_ext commit `0cdec98`, on the same
RTX 5090 / Linux 6.15.11 / NVIDIA 575.57.08 / 400 W host as GPreempt.
Other compilation was paused and the runner held both shared experiment leases.
No driver or service was changed.

- [Real kernel/copy profile](raw/profile-575-01/profile.json): 308 runtime
  operations, including the executor's no-op copy, profiled using complete
  ResNet152 passes and occupancy-based capacity search. All 23 ResNet152 and
  102 VGG19 calibration outputs matched the isolated reference, maximum
  absolute error zero. The recorded source launch list has 307 entries; the
  runtime's additional operation is retained, not dropped from execution.
- The median measured launch cost is 1,298 ns. The frozen large-bubble threshold
  is 24,108 ns, derived from isolated copy/sync durations by the planned rule.
  Those measurements do not establish safe small-bubble admission; the initial
  profile has both small patterns disabled pending the separate comparison.
- [Isolated foreground cell](raw/isolated-lc-575-01/isolated-lc/result.json):
  all 6,000 periodic 100 requests/s arrivals completed and passed numerical
  checks inside the 60-second window, with no backlog. Scheduled-arrival to
  synchronized, verified output p99 is **1,811,879 ns**, frozen as the common SLO.
  Warmup/calibration adds 110 checked outputs, excluded from that p99.

Both runs exited successfully, passed telemetry and GPU/struct-ops cleanup,
and retained raw requests, commands and explicit binary inventories. These are
calibration dependencies, not performance comparison samples.

## Small-pattern qualification

All 20 cells (four variants, five randomized paired 10-second blocks) passed
[independent raw audit](raw/small-pattern-575-01/independent-audit.json), with no
missing, rejected or unexpected attempts. Every foreground request completed
correctly inside its window. The separate [selection record](raw/small-pattern-575-01/small-pattern-results.json)
retains all variants, including ineligible ones.

- Output-only qualified: real output-pattern launches occurred in every block;
  its paired LC-p99 ratio against no small patterns was 0.96859, 95% interval
  [0.91917, 1.00252], below the predeclared 1.01 upper-bound limit.
- Input-only and both were ineligible: neither observed an actual input-pattern
  launch. This is not successful input-bubble filling, even where latency was
  similar. No cell was rerun or dropped to change that outcome.

The [selected profile](raw/small-pattern-575-01/profile-selected.json) enables
output only. This qualification is an incremental-interference check on this
frontend, not a throughput benefit or a final C/BPF comparison. The shared
executor conservatively waits for each LP launch's completion event before
issuing the next; that differs from the paper's predicted pipeline.

## Real five-arm preflight

All ten 10-second cells completed: two arrival cases and five arms, with the
same selected profile, numerical checks, telemetry and cleanups. All 5,460
offered foreground requests finished correctly inside their windows. The
BurstGPT preflight preserves the first ten seconds of the frozen trace (92
arrivals); it does not compress the full 6,000-arrival trace into ten seconds.

Actual host-BPF JIT decision counts were 32,304,292 for periodic arrivals and
30,319,590 for BurstGPT. Every recorded BPF decision used the JIT; C cells
recorded zero JIT calls. Request, CTA and no-op-copy counts were retained in
the [raw cells](raw/preflight-575-01/completed-cells.json).

The short observations do **not** show a background-throughput recovery over
fixed GPreempt. That is not a reason to discard the cells or weaken correctness.
The full comparison must quantify the cost and foreground tradeoff. Shared
completion/pacing work is a candidate explanation, not a measured causal
breakdown: event-wait counters overlap HP-stop and tick-wait conditions.

## Commands and next measurements

The completed commands below use retained output paths; replays must use new
paths on the prepared idle host:

```bash
sudo -n python3 -B workloads/hummingbird/run_study.py profile \
  --output workloads/hummingbird/raw/profile-575-01 --cell-timeout 900
sudo -n python3 -B workloads/hummingbird/run_study.py calibrate \
  --output workloads/hummingbird/raw/isolated-lc-575-01
sudo -n python3 -B workloads/hummingbird/run_study.py small-calibration \
  --output workloads/hummingbird/raw/small-pattern-575-01 \
  --profile workloads/hummingbird/raw/profile-575-01/profile.json --slo-ns 1811879
sudo -n python3 -B workloads/hummingbird/run_study.py preflight \
  --output workloads/hummingbird/raw/preflight-575-01 \
  --profile workloads/hummingbird/raw/small-pattern-575-01/profile-selected.json \
  --slo-ns 1811879 --cell-timeout 240
```

All four commands above completed. The same selected profile next feeds the
two-arrival-case, five-arm performance matrix after CPU build work finishes.
Small-pattern calibration and preflight are not pooled with final results.
