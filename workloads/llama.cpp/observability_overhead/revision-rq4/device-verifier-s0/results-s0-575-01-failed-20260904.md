# S0 attempt 01: retained invalid timing run

Date: 2026-09-04  
GPU / driver: NVIDIA GeForce RTX 5090 / 575.57.08  
Result directory: `raw/s0-575-01`

## Outcome

This attempt is **invalid and contributes no performance result**. The runner
stopped at timing sequence 12 with `status=invalid_timing`, as required by the
fail-closed protocol. All six correctness cells and the first five timing cells
passed. The sixth timing cell, the block-1 `threadhist` control, completed the
real workload successfully but failed the output-consistency gate.

The rejected row reports:

- `avg_ns=13,425,897`;
- `avg_ts=38,135.254576` token/s; and
- `samples_ts[0]=38,135.3` token/s.

These values are mathematically consistent: `512 * 1e9 / 13,425,897` gives the
unrounded average, while `llama-bench` emits each `samples_ts` value through a
default C++ stream with six significant digits. The harness incorrectly used
an absolute tolerance of 0.001 token/s for that rounded field; the observed
0.045424 token/s difference is within the 0.05 token/s rounding interval at
this magnitude. This is a harness precision defect, not a workload or GPU
failure.

## Evidence boundary

The raw prefix is retained so the failed attempt cannot be silently replaced.
It contains 12 unique target-process identities, the exact frozen schedule
prefix, per-cell commands and environments, admission records, device probe
readbacks, artifact metadata, telemetry, and safety cleanup. No partial timing
number from this attempt is used in the S0 paired analysis.

Before retrying, both the runner and independent analyzer must validate
`samples_ts` against the documented six-significant-digit representation, add
positive and negative boundary tests, and continue to validate `avg_ts`
independently against `pp * 1e9 / avg_ns`.
