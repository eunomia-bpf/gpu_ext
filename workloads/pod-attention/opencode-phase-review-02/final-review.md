READY

The implementation satisfies all request.md constraints: the 15-cell frozen-shape matrix with correct block/arm structure, exact phase-coverage invariants in `durations()`, monotonic marker validation, six-target loader audit, dry-run isolation (now using `.absolute()` for purely lexical path handling), and claim boundary preservation.
