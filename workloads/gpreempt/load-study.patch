diff --git a/CMakeLists.txt b/CMakeLists.txt
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -1,6 +1,11 @@
 project(GPreempt)
 cmake_minimum_required(VERSION 3.10)
 set(CMAKE_EXPORT_COMPILE_COMMANDS ON)
+option(GPREEMPT_LOAD_STUDY "Isolated FIFO load study with complete arrival accounting" OFF)
+if(GPREEMPT_LOAD_STUDY)
+    add_definitions(-DGPREEMPT_LOAD_STUDY)
+    include_directories("${GPREEMPT_ARTIFACT_DIR}")
+endif()
 
 set(CMAKE_CXX_STANDARD 17)
 set(CMAKE_CXX_FLAGS "-O3 ${CMAKE_CXX_FLAGS}")
diff --git a/third_party/disb/include/benchmark.h b/third_party/disb/include/benchmark.h
--- a/third_party/disb/include/benchmark.h
+++ b/third_party/disb/include/benchmark.h
@@ -35,6 +35,9 @@
 #include <memory>
 #include <string>
 #include <json/json.h>
+#ifdef GPREEMPT_LOAD_STUDY
+#include "load_study_measurement.h"
+#endif
 
 #define WARMUP_TIME                         10
 #define STANDALONE_LATENCY_MEASURE_TIME     100
@@ -51,6 +54,11 @@
     std::shared_ptr<Client> client;
     std::shared_ptr<Load> load;
     std::shared_ptr<StandAloneLatency> standAloneLatency;
+#ifdef GPREEMPT_LOAD_STUDY
+    std::shared_ptr<gpreempt_load_study::CommonStart> studyStart;
+    int64_t studyIntervalNs = 0;
+    int64_t studyVerifiedReadyNs = 0;
+#endif
 
     BenchmarkTask(): id("UNKNOWN") {}
     BenchmarkTask(std::string taskId, std::shared_ptr<Client> clt, std::shared_ptr<Load> stg);
diff --git a/third_party/disb/src/benchmark.cpp b/third_party/disb/src/benchmark.cpp
--- a/third_party/disb/src/benchmark.cpp
+++ b/third_party/disb/src/benchmark.cpp
@@ -33,6 +33,7 @@
 #include <iostream>
 #include <json/json.h>
 #include <atomic>
+#include <cstdio>
 
 namespace DISB
 {
@@ -116,6 +117,9 @@
     for (auto record : records) record->analyzer->onPostprocess(record);
     timePoints->postprocessBegin = system_clock::now();
     client->postprocess();
+#ifdef GPREEMPT_LOAD_STUDY
+    studyVerifiedReadyNs = gpreempt_load_study::now_ns();
+#endif
     timePoints->postprocessEnd = system_clock::now();
 
     // give the record back to the analyzer produced it
@@ -141,6 +145,50 @@
                                  system_clock::time_point endTime)
 {
     client->initInThread();
+#ifdef GPREEMPT_LOAD_STUDY
+    const auto window = studyStart->wait();
+    beginTime = window.wall_begin;
+    endTime = window.wall_end;
+    client->startAnalyzers(beginTime);
+    gpreempt_load_study::Schedule schedule(window.begin_ns, window.end_ns, studyIntervalNs);
+    std::vector<gpreempt_load_study::Request> requests;
+    requests.reserve(studyIntervalNs ? schedule.offered() : 65536);
+    while (true) {
+        std::this_thread::sleep_until(gpreempt_load_study::at_ns(schedule.next_ns()));
+        gpreempt_load_study::Request request;
+        // Recheck after sleeping. At end, no new admission, even if FIFO is backlogged.
+        if (!schedule.admit(gpreempt_load_study::now_ns(), request)) break;
+        inferOnce();
+        request.verified_ready_ns = studyVerifiedReadyNs;
+        requests.push_back(request);
+    }
+    client->stopAnalyzers(endTime);
+    Json::Value study;
+    study["task"] = id;
+    study["clock"] = "steady_clock";
+    study["mode"] = studyIntervalNs ? "periodic_fifo" : "continuous_closed_loop";
+    study["phase_ns"] = Json::Int64(0);
+    study["begin_ns"] = Json::Int64(window.begin_ns);
+    study["end_ns"] = Json::Int64(window.end_ns);
+    study["interval_ns"] = Json::Int64(studyIntervalNs);
+    study["offered"] = studyIntervalNs ? Json::Value(Json::UInt64(schedule.offered())) : Json::Value();
+    study["started"] = Json::UInt64(schedule.started());
+    study["request_fields"] = "id,scheduled_ns,started_ns,verified_ready_ns";
+    study["requests"] = Json::Value(Json::arrayValue);
+    for (const auto &request : requests) {
+        Json::Value row(Json::arrayValue);
+        row.append(Json::UInt64(request.id));
+        row.append(Json::Int64(request.scheduled_ns));
+        row.append(Json::Int64(request.started_ns));
+        row.append(Json::Int64(request.verified_ready_ns));
+        study["requests"].append(row);
+    }
+    Json::StreamWriterBuilder writer;
+    writer["indentation"] = "";
+    const auto line = Json::writeString(writer, study);
+    // One locked stdio write per task, after all timed requests, avoids interleaving.
+    fprintf(stderr, "GPREEMPT_LOAD_STUDY %s\n", line.c_str());
+#else
     initializedTaskCount.fetch_add(1);
     while(initializedTaskCount.load() < taskCount);
     auto delta = system_clock::now() + seconds(DELAY_BEGIN_SECONDS) - beginTime;
@@ -156,6 +204,7 @@
 
     load->stop();
     client->stopAnalyzers(endTime);
+#endif
 }
 
 BenchmarkSuite::BenchmarkSuite()
@@ -197,6 +246,18 @@
             client->initAnalyzers();
             load->init();
             tasks[taskId] = BenchmarkTask(taskId, client, load);
+#ifdef GPREEMPT_LOAD_STUDY
+            const auto &arrival = taskConfig["load"];
+            const auto mode = arrival["type"].asString();
+            if (mode == "periodic_fifo") {
+                const int rate = arrival["frequency"].asInt();
+                if (rate <= 0 || 1000000000 % rate != 0)
+                    throw std::invalid_argument("FIFO rate must divide one second");
+                tasks[taskId].studyIntervalNs = 1000000000 / rate;
+            } else if (mode != "continuous") {
+                throw std::invalid_argument("load-study requires periodic_fifo or continuous");
+            }
+#endif
         } 
     }
 
@@ -304,7 +365,9 @@
         auto &task = taskPair.second;
         loadInfos.emplace_back(task.load, task.standAloneLatency);
     }
+#ifndef GPREEMPT_LOAD_STUDY
     loadCoordinator(loadInfos);
+#endif
 
     // 4. launch benchmark
     beginTime = system_clock::now() + seconds(DELAY_BEGIN_SECONDS);
@@ -322,14 +385,26 @@
     }
 
     taskCount = tasks.size();
+#ifdef GPREEMPT_LOAD_STUDY
+    auto studyStart = std::make_shared<gpreempt_load_study::CommonStart>(taskCount,
+        std::chrono::duration_cast<nanoseconds>(benchmarkTime).count(),
+        std::chrono::duration_cast<nanoseconds>(seconds(DELAY_BEGIN_SECONDS)).count());
+#endif
     for (auto &taskPair : tasks) {
         auto &task = taskPair.second;
+#ifdef GPREEMPT_LOAD_STUDY
+        task.studyStart = studyStart;
+#endif
         benchmarkThreads.emplace_back(&BenchmarkTask::runBenchmark, task, beginTime, endTime);
     }
     
     for (auto &thread : benchmarkThreads) {
         thread.join();
     }
+#ifdef GPREEMPT_LOAD_STUDY
+    beginTime = studyStart->window().wall_begin;
+    endTime = studyStart->window().wall_end;
+#endif
 
     if(daemonThreadCreated) {
         stopFlag = true;
@@ -343,6 +418,12 @@
     report["beginTime"] = timepointToString(beginTime);
     report["endTime"] = timepointToString(endTime);
     report["benchmarkTime(s)"] = (Json::Value::Int64) benchmarkTime.count();
+#ifdef GPREEMPT_LOAD_STUDY
+    const auto window = tasks.begin()->second.studyStart->window();
+    report["loadStudyBeginNs"] = Json::Int64(window.begin_ns);
+    report["loadStudyEndNs"] = Json::Int64(window.end_ns);
+    report["loadStudyClock"] = "steady_clock";
+#endif
 
     Json::Value results;
     for (auto &taskPair : tasks) {
@@ -353,4 +434,4 @@
     return report;
 }
 
-}
\ No newline at end of file
+}
diff --git a/third_party/disb/src/load.cpp b/third_party/disb/src/load.cpp
--- a/third_party/disb/src/load.cpp
+++ b/third_party/disb/src/load.cpp
@@ -70,6 +70,11 @@
 
 std::shared_ptr<Load> builtinLoadFactory(const Json::Value &config)
 {
+#ifdef GPREEMPT_LOAD_STUDY
+    // BenchmarkTask supplies the FIFO scheduler; this object is never coordinated/run.
+    if (config["type"].asString() == "periodic_fifo")
+        return std::make_shared<PeriodicLoad>(config);
+#endif
     std::string type = config["type"].asString();
 
     if (type == "continuous") {
diff --git a/src/common-clients/baseclient.cpp b/src/common-clients/baseclient.cpp
--- a/src/common-clients/baseclient.cpp
+++ b/src/common-clients/baseclient.cpp
@@ -1,5 +1,6 @@
 #include "disb.h"
 #include "client_checks.h"
+#include "load_study_measurement.h"
 #include "gpreempt_bridge.h"
 
 #include <string>
@@ -43,6 +44,10 @@
     
     virtual void initInThread() override {
         bind_core(cores.fetch_add(1));
+#ifdef GPREEMPT_LOAD_STUDY
+        // A CUDA current context is per host thread; bind the same primary context.
+        foo::util::init_cuda();
+#endif
         checks.begin_timed();
     }
 
@@ -56,15 +61,24 @@
         CHECK_STATUS(executor->init(model_name));
         input_size = executor->get_data_size("data");
         output_size = executor->get_data_size("heads");
+#ifndef GPREEMPT_LOAD_STUDY
         GPUStreamCreate(&stream, 0);
+#endif
         ASSERT_CUDA_ERROR(GPUMemHostAlloc(&input, input_size, CU_MEMHOSTALLOC_PORTABLE));
         ASSERT_CUDA_ERROR(GPUMemHostAlloc(&output, output_size, CU_MEMHOSTALLOC_PORTABLE));
         checks.initialize(getName(), std::string(MODEL_PATH) + "/" + model_name + "/reference.f32",
                           input, input_size, output_size);
 
         int least_priority = 0, greatest_priority = 0; 
-        GPUCtxGetStreamPriorityRange(&least_priority, &greatest_priority);
-        GPUStreamCreateWithPriority(&stream, 0, priority ? least_priority : greatest_priority);
+        GPREEMPT_STUDY_CUDA(GPUCtxGetStreamPriorityRange(&least_priority, &greatest_priority));
+        GPREEMPT_STUDY_CUDA(GPUStreamCreateWithPriority(&stream, 0, priority ? least_priority : greatest_priority));
+#ifdef GPREEMPT_LOAD_STUDY
+        int actual_priority = 0;
+        ASSERT_CUDA_ERROR(cuStreamGetPriority(stream, &actual_priority));
+        CHECK_EQ(actual_priority, priority ? least_priority : greatest_priority);
+        fprintf(stderr, "GPREEMPT_LOAD_PRIORITY task=%s role=%d actual=%d least=%d greatest=%d\n",
+                getName().c_str(), priority, actual_priority, least_priority, greatest_priority);
+#endif
         if(use_cuda_graph){
             executor->capture_graph(stream);
         }
@@ -80,19 +94,19 @@
     virtual void copyInput() override
     {
         CHECK_STATUS(executor->set_input("data", input, input_size, stream));
-        GPUStreamSynchronize(stream);
+        GPREEMPT_STUDY_CUDA(GPUStreamSynchronize(stream));
     }
 
     virtual void infer() override
     {
         CHECK_STATUS(executor->execute(stream));
-        GPUStreamSynchronize(stream);
+        GPREEMPT_STUDY_CUDA(GPUStreamSynchronize(stream));
     }
 
     virtual void copyOutput() override
     {
         CHECK_STATUS(executor->get_output("heads", output, output_size, stream));
-        GPUStreamSynchronize(stream);
+        GPREEMPT_STUDY_CUDA(GPUStreamSynchronize(stream));
     }
 
     virtual void postprocess() override { checks.check(output, output_size); }
diff --git a/src/cuda-clients/gpreemptclient.cpp b/src/cuda-clients/gpreemptclient.cpp
--- a/src/cuda-clients/gpreemptclient.cpp
+++ b/src/cuda-clients/gpreemptclient.cpp
@@ -15,6 +15,7 @@
 #include "gpreempt.h"
 #include "block.h"
 #include "client_checks.h"
+#include "load_study_measurement.h"
 #include "gpreempt_bridge.h"
 
 #include "flag_transport.h"
@@ -123,7 +124,7 @@
         CHECK_STATUS(executor->init(model_name));
         input_size = executor->get_data_size("data");
         output_size = executor->get_data_size("heads");
-        GPUStreamCreate(&stream, 0);
+        GPREEMPT_STUDY_CUDA(GPUStreamCreate(&stream, 0));
         ASSERT_CUDA_ERROR(GPUMemHostAlloc(&input, input_size, CU_MEMHOSTALLOC_PORTABLE));
         ASSERT_CUDA_ERROR(GPUMemHostAlloc(&output, output_size, CU_MEMHOSTALLOC_PORTABLE));
         checks.initialize(getName(), std::string(MODEL_PATH) + "/" + model_name + "/reference.f32",
@@ -139,12 +140,16 @@
     virtual void initInThread() override 
     {
         bind_core(cores.fetch_add(1));
+#ifdef GPREEMPT_LOAD_STUDY
+        // Warmup resources belong to the shared primary context on the main thread.
+        foo::util::init_cuda();
+#endif
         executor->clear();
         GPUMemFreeHost(input);
         GPUMemFreeHost(output);
-        GPUStreamDestroy(stream);
+        GPREEMPT_STUDY_CUDA(GPUStreamDestroy(stream));
         GPUdevice dev;
-        GPUDeviceGet(&dev, 0);
+        GPREEMPT_STUDY_CUDA(GPUDeviceGet(&dev, 0));
         mtx.lock();
         if(g_ctx[priority] == nullptr) {
             CHECK_EQ(gpreempt_ctx_begin(priority), 0);
@@ -160,13 +165,13 @@
                                            nvctx.hClient, nvctx.hObject, priority), 0);
             CHECK_EQ(gpreempt_ctx_end(), 0);
         }
-        GPUCtxSetCurrent(g_ctx[priority]);
+        GPREEMPT_STUDY_CUDA(GPUCtxSetCurrent(g_ctx[priority]));
         if(!priority) {
             int res = get_gdr_map(&gdr_stop);
             CHECK_EQ(res, 0); // Also fatal in Release builds; never fall back.
         }
-        GPUStreamCreate(&stream, 0);
-        GPUStreamCreate(&copy_stream, 0);
+        GPREEMPT_STUDY_CUDA(GPUStreamCreate(&stream, 0));
+        GPREEMPT_STUDY_CUDA(GPUStreamCreate(&copy_stream, 0));
         if (!priority) flag_pool.track_stream(stream);
         CHECK_STATUS(executor->init(model_name));
         ASSERT_CUDA_ERROR(GPUMemHostAlloc(&input, input_size, CU_MEMHOSTALLOC_PORTABLE));
@@ -214,10 +219,10 @@
     {
         if(initialized) {
             CHECK_STATUS(executor->set_input("data", input, input_size, copy_stream));
-            GPUStreamSynchronize(copy_stream);
+            GPREEMPT_STUDY_CUDA(GPUStreamSynchronize(copy_stream));
         } else {
             CHECK_STATUS(executor->set_input("data", input, input_size, stream));
-            GPUStreamSynchronize(stream);
+            GPREEMPT_STUDY_CUDA(GPUStreamSynchronize(stream));
         }
     }
 
@@ -230,13 +235,13 @@
         if(action & GP_RELEASE) {
             end_blocking_stream(&gdr_stop);
         }
-        GPUStreamSynchronize(stream);
+        GPREEMPT_STUDY_CUDA(GPUStreamSynchronize(stream));
     }
 
     virtual void copyOutput() override
     {
         CHECK_STATUS(executor->get_output("heads", output, output_size, stream));
-        GPUStreamSynchronize(stream);
+        GPREEMPT_STUDY_CUDA(GPUStreamSynchronize(stream));
     }
 
     virtual void postprocess() override { checks.check(output, output_size); }
@@ -265,7 +270,7 @@
     while(g_ctx[0] == nullptr) {
         std::this_thread::sleep_for(std::chrono::milliseconds(1));
     }
-    GPUCtxSetCurrent(g_ctx[0]);
+    GPREEMPT_STUDY_CUDA(GPUCtxSetCurrent(g_ctx[0]));
 
     std::set<DeamonHint> hints;
     DeamonHint *nu = nullptr;
diff --git a/src/executor.cpp b/src/executor.cpp
--- a/src/executor.cpp
+++ b/src/executor.cpp
@@ -1,4 +1,5 @@
 #include "executor.h"
+#include "load_study_measurement.h"
 #include "model.h"
 
 #include "util/gpu_util.h"
@@ -175,11 +176,11 @@
 
 void Executor::capture_graph(GPUstream stream){
     use_cuda_graph = true;
-    GPUGraphCreate(&graph, 0);
-    GPUStreamBeginCapture(stream, GPU_STREAM_CAPTURE_MODE_GLOBAL);
+    GPREEMPT_STUDY_CUDA(GPUGraphCreate(&graph, 0));
+    GPREEMPT_STUDY_CUDA(GPUStreamBeginCapture(stream, GPU_STREAM_CAPTURE_MODE_GLOBAL));
     if(type == "dnn") {
         for (size_t i = 0; i < get_kernel_num(); i++) {
-            launch_kernel(i, stream);
+            GPREEMPT_STUDY_STATUS(launch_kernel(i, stream));
         }
     } else if(type == "graph") {
         use_cuda_graph = false;
@@ -187,11 +188,11 @@
     } else {
         SciComputeRaw::perform_timestep(stream);
     }
-    GPUStreamEndCapture(stream, &graph);
+    GPREEMPT_STUDY_CUDA(GPUStreamEndCapture(stream, &graph));
 #ifdef CUDA
-    GPUGraphInstantiate(&graph_exec, graph, 0);
+    GPREEMPT_STUDY_CUDA(GPUGraphInstantiate(&graph_exec, graph, 0));
 #else
-    GPUGraphInstantiate(&graph_exec, graph, NULL, NULL, 0);
+    GPREEMPT_STUDY_CUDA(GPUGraphInstantiate(&graph_exec, graph, NULL, NULL, 0));
 #endif
 }
 
