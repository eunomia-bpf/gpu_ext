# AE artifact audit status — 2026-09-10

This is the end-of-window record for the artifact-review / AE-preparation
audit requested against `ARTIFACT.md`. It records what is ready, what is
measured, what is unverified, and the actual blockers. It does not replace
the dated campaign reports and adds no new claims beyond them. The deadline
(17:53 UTC) was met by this record; no new GPU campaign was started because
no open XSched cell remained that would not duplicate already-recorded
results or disturb concurrent lanes.

## AE-ready (evaluator can execute, no GPU)

- `python3 -B scripts/artifact/reanalyze.py --campaign all` recomputes all
  five published campaigns from their raw `result.json` cells (15+15+25+15+20
  = 90 cell summaries). Verified on `371f940e`: exit 0, every campaign's
  recomputed medians cross-check against its committed
  `summary.json`/`paired-analysis.json` (storage matches `analysis.json`;
  write-budget matches `paired-analysis.json`; MoE matches
  `audited-analysis-final.json`; both XSched campaigns match their
  `summary.json`).
- `python3 -B scripts/artifact/reproduce_figures.py --out-dir NEW` regenerates
  the paper-selected observability figure and the seven-panel matched-policy
  figure (5.572554% selected five-block overhead; 5.400619% retained
  ten-block extension). Verified on `371f940e`: exit 0, both PDF/PNG pairs
  written outside `docs/paper`.
- All relative links in `ARTIFACT.md` and the `docs/artifact/` entrypoints
  resolve (119 checked, 0 broken).
- Pinned public sources are in place: `git submodule status` shows libbpf,
  bpftool, nvidia-module, faiss, llama.cpp, vllm, and the paper submodule
  populated at pinned revisions.
- XSched Level-2 runtime lane: the reproducible nine-patch source recipe
  (`workloads/xsched/level2/native/prepare_native_source.sh`, committed in
  `013427f5`) reproduces the validated source byte-for-byte (9/9
  `CHAIN_FINAL` files `MATCH` on re-run, recorded in
  `workloads/xsched/level2/native/README.md`); the compiled
  `install-gcc` tree it produced (HAL, shim `libshimcuda.so`, `bin/xserver`)
  is retained under
  `workloads/xsched/level2-build/.output/native-repro-supervisor-20260910.wEUkbn/`,
  with `libcuda.so.1` shim-linked per `docs/artifact/xsched-native-runtime.md`.

## Measured evidence (in the tree, cross-checked above)

- Original-actuator matched gap is closed by data, not by a new run:
  `level2-native-route-20260910-082400` (20 cells, 5 blocks x 4 arms, all
  engagement gates passing) records LC service p99 medians
  baseline 2,079,286.752 µs / l1_native 1,195,381.632 µs /
  l2_cuxtra 960,347.488 µs / l2_bpfhost 960,418.112 µs, i.e. the original
  cuXtra SASS actuator (-53.9% vs baseline) and its BPF-host variant
  (median of paired ratios -0.057% vs native cuXtra). This is the
  original-system route, run on the nine-patch source install.
- The shared-actuator policy-port campaign
  (`level2-device-policy-pair-20260909.buBjns`, 15 cells) remains a separate
  result (BPF/native service-p99 paired median +13.51%); both records and
  `docs/artifact/reanalysis.md` state the distinction explicitly.

## Unverified / not demonstrated here

- The nine-patch install is a built and runtime-validated source recipe,
  not a tested fresh-OS installer; `ARTIFACT.md` open items keep that
  boundary.
- Per-workload fresh-checkout build/run instructions are not all
  demonstrated from a clean machine; recorded runs are the evidence, and
  `ARTIFACT.md` open items retain that.
- The complete paper is not yet a one-command rerun beyond the five
  campaigns and two figures above (`ARTIFACT.md` open item).

## Actual blockers

- Original agent-study transcripts: genuinely missing. The 25 primary /
  259 nested originals remain unrecovered (inventory,
  `docs/experiment/revision-artifact-inventory.md`); the published
  `reproduction-prompts.md` are newly authored templates and are labelled
  as such. This stays honestly missing, not reconstructed, until the author
  supplies the backup.
- No GPU blocker in the XSched lane: the original-actuator comparison was
  already complete when this window opened, so no further measurement was
  required or run.

## Concurrent work preserved

The MoE/LMCache/PoD lanes' in-flight edits (e.g.
`workloads/moe-infinity/`, untracked `platforms/`) were left untouched; this
audit changed only the artifact documentation entrypoints and this file.
